import './Children.css'

export default function Children({children}){ //apenas criando espaço para a children

    return(
        <div className="children">
            {children}
        </div>
    )
}